@model AssignedHomework

@{
    ViewData["Title"] = "Ödev Yükle";
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <h2 class="text-center mb-4">📤 Ödev Yükle</h2>

        @if (Model == null)
        {
            <div class="alert alert-danger">
                <strong>Hata!</strong> Ödev bulunamadı!
            </div>
            <a asp-action="Index" class="btn btn-secondary">Geri Dön</a>
        }
        else
        {
            <div class="card shadow-sm mb-4">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">📋 Ödev Detayları</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-8">
                            <h5 class="card-title text-primary">@Model.Title</h5>
                            <p class="card-text text-muted">@Model.Description</p>
                        </div>
                        <div class="col-md-4 text-end">
                            <small class="text-muted d-block">Son Teslim Tarihi</small>
                            <strong class="text-danger">@Model.DueDate.ToString("dd/MM/yyyy")</strong>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0">📁 Dosya Yükle</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Yukle" method="post" enctype="multipart/form-data">
                        <input type="hidden" name="id" value="@Model.Id" />
                        
                        <div class="mb-3">
                            <label for="dosya" class="form-label fw-bold">Dosyanızı Seçin:</label>
                            <input type="file" name="dosya" id="dosya" class="form-control form-control-lg" required accept=".pdf,.doc,.docx,.zip" />
                            <div class="form-text">Kabul edilen formatlar: PDF, Word, ZIP</div>
                        </div>
                        
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-action="Index" class="btn btn-secondary me-md-2">
                                <i class="bi bi-arrow-left"></i> İptal
                            </a>
                            <button type="submit" class="btn btn-success btn-lg">
                                <i class="bi bi-cloud-upload"></i> Yükle
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        }
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
